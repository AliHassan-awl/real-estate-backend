import'dotenv/config';import{PrismaClient}from'@prisma/client';import{v2 as cloudinary}from'cloudinary';
export const prisma=new PrismaClient();export const config={port:Number(process.env.PORT)||5000,nodeEnv:process.env.NODE_ENV||'development',jwtSecret:process.env.JWT_SECRET||'development-only-secret-change-me-123456',jwtExpires:process.env.JWT_EXPIRES_IN||'8h',frontends:(process.env.FRONTEND_URL||'http://localhost:5173').split(',').map(x=>x.trim())};cloudinary.config({cloud_name:process.env.CLOUDINARY_CLOUD_NAME,api_key:process.env.CLOUDINARY_API_KEY,api_secret:process.env.CLOUDINARY_API_SECRET});export{cloudinary};
